---
phase: 01-environment-workflow-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - pyproject.toml
  - scripts/verify_phase1.sh
  - scripts/run_service.sh
  - .planning/phases/01-environment-workflow-baseline/01-VALIDATION.md
  - .planning/phases/01-environment-workflow-baseline/01-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - CORE-04
  - CORE-05
  - DEV-02
must_haves:
  truths:
    - "User can run the FastAPI service through an explicit `uv run` command without editing code."
    - "Phase 1 verification includes an automated check that the service boot command actually starts the app process."
    - "Locked `.env` startup decisions remain intact while closing the CORE-05 run-path gap."
  artifacts:
    - path: "pyproject.toml"
      provides: "Runtime dependency list includes uvicorn for `uv run` service execution"
      contains: "uvicorn==0.44.0"
    - path: "scripts/run_service.sh"
      provides: "Canonical service startup command using uv"
      contains: "uv run uvicorn src.main:app --host 127.0.0.1 --port 8000"
    - path: "scripts/verify_phase1.sh"
      provides: "Gap-closing verification sequence including service boot smoke check"
      contains: "uv run python -c"
  key_links:
    - from: "scripts/run_service.sh"
      to: "src/main.py"
      via: "uvicorn app import path"
      pattern: "src\.main:app"
    - from: "scripts/verify_phase1.sh"
      to: "scripts/run_service.sh"
      via: "command consistency check"
      pattern: "rg .*run_service\.sh"
---

<objective>
Close the Phase 1 verification gap by making service execution via `uv run` explicit, dependency-complete, and automatically verifiable.

Purpose: Satisfy CORE-05 truth #2 from 01-VERIFICATION.md without regressing CORE-04 or DEV-02 behavior.
Output: Updated dependency/runtime scripts and verification docs that prove the service can be started with `uv` commands.
</objective>

<execution_context>
@/Users/chengtong/OpenSource/myagent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chengtong/OpenSource/myagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-workflow-baseline/01-CONTEXT.md
@.planning/phases/01-environment-workflow-baseline/01-RESEARCH.md
@.planning/phases/01-environment-workflow-baseline/01-VERIFICATION.md
@.planning/phases/01-environment-workflow-baseline/01-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add uvicorn runtime dependency and canonical `uv run` service entry command</name>
  <read_first>pyproject.toml, src/main.py, .planning/phases/01-environment-workflow-baseline/01-VERIFICATION.md, .planning/phases/01-environment-workflow-baseline/01-CONTEXT.md</read_first>
  <files>pyproject.toml, scripts/run_service.sh</files>
  <action>Update `pyproject.toml` to include `uvicorn==0.44.0` in main dependencies to close the missing runtime executable called out by verification gap reason. Create `scripts/run_service.sh` (chmod +x) with exactly one startup command: `uv run uvicorn src.main:app --host 127.0.0.1 --port 8000`. Keep `.env` contract behavior untouched and continue using startup-validated settings per D-01, D-03, D-04, and D-05; do not add `.env.example` per D-02.</action>
  <verify>
    <automated>uv sync && uv run uvicorn --version && rg -n "^uv run uvicorn src.main:app --host 127.0.0.1 --port 8000$" scripts/run_service.sh</automated>
  </verify>
  <acceptance_criteria>`pyproject.toml` contains `uvicorn==0.44.0`; `scripts/run_service.sh` exists, is executable, and contains the exact canonical startup command with `src.main:app` import target.</acceptance_criteria>
  <done>Dependency and command artifacts exist so users have an unambiguous `uv run` service startup path.</done>
</task>

<task type="auto">
  <name>Task 2: Extend phase verification to prove service boot path and preserve milestone traceability</name>
  <read_first>scripts/verify_phase1.sh, scripts/run_service.sh, .planning/phases/01-environment-workflow-baseline/01-VALIDATION.md, .planning/phases/01-environment-workflow-baseline/01-VERIFICATION.md</read_first>
  <files>scripts/verify_phase1.sh, .planning/phases/01-environment-workflow-baseline/01-VALIDATION.md, .planning/phases/01-environment-workflow-baseline/01-VERIFICATION.md</files>
  <action>Update `scripts/verify_phase1.sh` to run, in order: `uv sync`, `uv run pytest tests/test_settings.py tests/test_startup.py -q -x`, bounded service-boot smoke check (`uv run python -c "import subprocess,time; p=subprocess.Popen(['uvicorn','src.main:app','--host','127.0.0.1','--port','8011']); time.sleep(2); running=(p.poll() is None); p.terminate(); p.wait(timeout=5); raise SystemExit(0 if running else 1)"`), canonical command consistency grep against `scripts/run_service.sh`, and existing git checkpoint grep for DEV-02. Update `01-VALIDATION.md` Per-Task Verification Map to include the new service run check under CORE-05. Update `01-VERIFICATION.md` gap status to closed with concrete evidence fields and commands. Do not alter env decision scope (D-01..D-05) while editing docs.</action>
  <verify>
    <automated>bash scripts/verify_phase1.sh</automated>
  </verify>
  <acceptance_criteria>`bash scripts/verify_phase1.sh` exits 0, includes an automated service-process boot assertion, and both validation/verification markdown files explicitly reference the same concrete run command and closure evidence.</acceptance_criteria>
  <done>CORE-05 gap is closed with automated proof, and DEV-02 traceability checks remain part of the same reproducible verification entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell -> service bootstrap script | Command-line input executes dependency sync and app startup commands. |
| `.env` -> startup settings validation | Untrusted local env values cross into typed startup validation path. |
| verification script -> git history output | Script output is used as milestone evidence and can be misleading if checks are weak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | `scripts/run_service.sh` | mitigate | Pin exact `uv run uvicorn src.main:app` command and grep-verify exact command text in `scripts/verify_phase1.sh`. |
| T-01-02-02 | D | `scripts/verify_phase1.sh` service smoke check | mitigate | Use bounded subprocess startup with forced terminate/wait timeout so verification cannot hang indefinitely. |
| T-01-02-03 | I | startup env handling | mitigate | Keep fail-fast typed settings path unchanged (D-01..D-05) and avoid printing `MODEL_API_KEY` in verification output. |
| T-01-02-04 | R | milestone traceability evidence | mitigate | Preserve explicit `git log ... | rg ...` checkpoint check in verification script and reflect exact command in validation docs. |
</threat_model>

<verification>
- Run `uv sync && uv run uvicorn --version` to confirm dependency and executable availability.
- Run `bash scripts/verify_phase1.sh` as the single automated closure gate.
- Confirm `01-VERIFICATION.md` records the CORE-05 truth as verified with command evidence.
</verification>

<success_criteria>
- User can execute `scripts/run_service.sh` and start the FastAPI app through `uv run` without modifying source code.
- `pyproject.toml` declares uvicorn runtime dependency so service startup command is reproducible after `uv sync`.
- Phase verification script proves service boot path plus existing tests and git milestone checks in one command.
- Locked decisions D-01 through D-05 remain fully satisfied with no scope reduction and no deferred ideas introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-workflow-baseline/01-02-SUMMARY.md`
</output>
