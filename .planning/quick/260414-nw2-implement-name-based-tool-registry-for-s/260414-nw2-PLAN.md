---
quick_id: 260414-nw2
description: Implement name-based tool registry for session bootstrap
date: 2026-04-14
---

# Quick Task Plan: Name-Based Tool Registry

## Plan 1: Tool Registry Implementation

### Task 1: Add ToolConfig/ToolSummary and update request/response models
- **files**: `src/core/config.py`
- **action**: Add `ToolConfig(name: str)` and `ToolSummary(name: str, description: str)` models. Add `tools: list[ToolConfig]` to `SessionBootstrapRequest`. Add `tools: list[ToolSummary]` to `SessionBootstrapResponse`.
- **verify**: Models importable with correct fields and validation.
- **done**: Config models support tool configuration in bootstrap request/response.

### Task 2: Create local tool registry and registration function
- **files**: `src/tools/__init__.py`
- **action**: Add `TOOL_REGISTRY` dict mapping name → function for existing tools. Add `ToolRegistryError(ValueError)`. Add `register_configured_tools(toolkit, tool_configs) -> list[ToolSummary]` that validates names (fail-fast), registers on toolkit, returns summaries.
- **verify**: Registry contains all 4 example tools. Registration works for valid names, raises for unknown.
- **done**: Local tool registry with registration function ready.

### Task 3: Wire registry into bootstrap and update response
- **files**: `src/agent/session_runtime.py`, `src/app/session_routes.py`
- **action**: Replace `create_base_toolkit(...)` with `Toolkit()` + `register_configured_tools()` in bootstrap. Catch `ToolRegistryError` as `SessionRuntimeValidationError`. Add `tool_summaries` to `SessionRuntime` dataclass. Pass `tools=runtime.tool_summaries` in route response.
- **verify**: Bootstrap with tools returns summaries. Unknown tool returns 400. Empty tools boots fine.
- **done**: Bootstrap path uses name-based tool registration.

### Task 4: Add tests
- **files**: `tests/test_tools.py`, `tests/test_session_bootstrap.py`
- **action**: Add `TestToolRegistry` class testing registry contents and `register_configured_tools` success/failure. Add bootstrap integration tests for tool success, unknown tool 400, empty tools.
- **verify**: All new tests pass. All existing 72 tests still pass.
- **done**: Full test coverage for tool registry feature.
