services:
  myagent:
    build:
      context: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: ${PORT:-8000}
      SESSION_BACKEND: json
      SESSION_DIR: /app/sessions
    volumes:
      - ./sessions:/app/sessions
